#include "Broker.hpp"

Broker::Broker()
{
}

int Broker::subscribe(const XIProducer& )//_producer)
{
    return -1;
}

int Broker::subscribe(const XIConsumer& )//_producer)
{
    return -1;
}

int Broker::unsubscribe(const XIProducer& )//_producer)
{
    return -1;
}

int Broker::unsubscribe(const XIConsumer& )//_producer)
{
    return -1;
}
